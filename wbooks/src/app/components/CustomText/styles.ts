import { StyleSheet } from 'react-native';
import { tundora } from '@constants/colors';
import { NORMAL_WEIGHT } from '@constants/fonts';

export default StyleSheet.create({
  text: {
    color: tundora,
    fontSize: 15,
    fontWeight: NORMAL_WEIGHT
  }
});
